<UserControl x:Class="Main.Views.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <ItemsControl Name="Items" ItemsSource="{Binding Path=VisibleItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Command="{Binding Path = DataContext.ToDoStateChangedCommand, ElementName=Items}" CommandParameter="{Binding}" Content="{Binding Name}" IsChecked="{Binding State, Mode=TwoWay}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
